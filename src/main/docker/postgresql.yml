version: '2'
services:
  sync-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/sync/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sync
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
